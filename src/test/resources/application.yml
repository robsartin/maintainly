spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/mystuffdb
    username: postgres
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

app:
  jwt:
    secret: test-only-hmac-secret-key-minimum-32-bytes-long!!
    expiration: 3600
  rate-limit:
    requests-per-second: 1000
    login-requests-per-second: 1000
  cors:
    allowed-origins:
      - http://localhost:8080
