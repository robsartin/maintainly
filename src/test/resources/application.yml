spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/maintainlydb
    username: postgres
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
